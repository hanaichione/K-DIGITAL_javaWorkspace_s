public class Student {
	// 클래스 : 변수선언, 함수선언
	// -> 클래스 이용, 객체 생성, 실제 학생의 정보를 객체마다 다르게 저장, 관리
	// ---- 클래스 : 틀
	// ---- 객체 : 실제 데이터 저장
		
		// 0. 멤버 변수, 인스턴스 변수, 전역 변수
		// 1. 선언 위치 : 클래스 블럭
		// 2. 그냥 사용 불가, 객체 생성 후 참조 변수를 이용 접근
		// ---- 클래스이름 변수 = new 클래스이름(); 
		// ----> 객체 생성, 각 변수 자동 초기화, 생성된 객체 주소를 변수에 참조 ex) t.name = "홍길동"; 객체에 접근 사용
		
		String name;
		int age;
		String address;
//		String phone;
		
		// 생성자(constructor) 영역
		// 함수영역, 메소드영역
}
